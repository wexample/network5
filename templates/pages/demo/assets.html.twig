{%- extends 'layouts/black.html.twig' -%}

{%- set template_path = _self -%}

{%- block page_body -%}
    <div class="temp-container">
        <div class="board-line">
            {{ template_name }}
        </div>
        {% for breakPoint,name in displayBreakpoints %}
            <div class="board-line display-breakpoint display-breakpoint-{{ breakPoint }}">
                <div class="pill size-name">{{ breakPoint }}</div>
                <div class="size-min">{{ name }}</div>
            </div>
        {% endfor %}
    </div>
{%- endblock -%}